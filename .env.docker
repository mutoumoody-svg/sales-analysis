# ============================================
# Docker Deployment Environment
# Copy this to .env and modify as needed
# ============================================

# Database password (must match what's in docker-compose.yml)
DB_PASSWORD=CHANGE_ME

# Backend settings
DEBUG=false
CORS_ORIGINS=*
